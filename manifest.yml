---
applications:
- name: ((APP_NAME))
  memory: 256M
  instances: 3
  command: python3 -m migrator
  health_check_type: process
  services:
  - ((CDN_DB_NAME))
  buildpacks:
  - python_buildpack
  env:
    ENV: ((ENV))
